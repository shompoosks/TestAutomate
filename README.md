# TestAutomate
Test Automate Robot + Apprium

Jenkins ดึงจาก branch: TA_robot
ต้องมี Python 3 + venv
Pipeline ทำ 5 stage: Set timestamp → Checkout → Install → Run Robot → Publish
run API test จาก run_api.sh